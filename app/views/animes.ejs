<html>
    <head>
        <title><%=siteTitle%>--<%=pageID%></title>
        <script src="/reload/reload.js"></script>
    </head>
    <body>
        <h2><%=pageTitle%></h2>
        <% animes.forEach(function(item) { %>
            <p><%= item.animeTitle %></p>
        
        <% }); %>
    </body>
</html>